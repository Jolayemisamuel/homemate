<!DOCTYPE html>
<html>
<head>
  <title><%= @title ? (@title + ' - ') : '' %>HomeMate</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <%= csrf_meta_tags %>
  <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
</head>

<body>
  <div class="d-flex container auth-container justify-content-center">
    <div class="auth-box m-md-1 h-100 align-self-center">
      <div class="d-flex justify-content-center">
        <img src="<%= asset_pack_path 'logo.png' %>" width="45%" />
      </div>
      <%= render "shared/alert", { notice: notice, alert: alert } %>
      <div class="mx-1 my-3 p-3 p-md-4 bg-white rounded">
        <%= yield %>
      </div>
      <div class="d-flex justify-content-center"><small class="text-muted">&copy; Andrew Ying 2017.</small></div>
    </div>
  </div>
</body>
</html>
